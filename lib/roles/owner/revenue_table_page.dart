/// Owner — revenue table: filter by taxi, driver, date range; export.
/// TODO: DataTable / PaginatedDataTable wired to FirestoreService revenue stream.
import 'package:flutter/material.dart';
class RevenueTablePage extends StatelessWidget {
  const RevenueTablePage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Revenue Table'));
}
