/// Owner — add a new taxi to their fleet.
/// TODO: form wired to FirestoreService.addTaxi.
import 'package:flutter/material.dart';
class AddTaxiPage extends StatelessWidget {
  const AddTaxiPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Add Taxi'));
}
