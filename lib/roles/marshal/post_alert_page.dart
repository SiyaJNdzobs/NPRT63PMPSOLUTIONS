/// Marshal — post a news or incident alert for the rank.
/// TODO: form wired to FirestoreService.postAlert + FCM notification.
import 'package:flutter/material.dart';
class PostAlertPage extends StatelessWidget {
  const PostAlertPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Post Alert'));
}
