/// Marshal — live queue view for the rank; dispatch controls.
/// TODO: stream from FirestoreService.streamQueue(rankId), dispatch action.
import 'package:flutter/material.dart';
class QueueViewPage extends StatelessWidget {
  const QueueViewPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Queue View'));
}
