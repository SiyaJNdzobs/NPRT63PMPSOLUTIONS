/// Admin — overview page showing aggregate stats across all owners/ranks.
/// TODO: implement stat cards, charts, live queue summary.
import 'package:flutter/material.dart';
class AdminOverviewPage extends StatelessWidget {
  const AdminOverviewPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Admin Overview'));
}
