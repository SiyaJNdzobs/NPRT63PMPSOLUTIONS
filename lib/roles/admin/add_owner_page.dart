/// Admin — add a single owner or trigger Excel bulk import.
/// TODO: form + file picker wired to ExcelImportService.
import 'package:flutter/material.dart';
class AddOwnerPage extends StatelessWidget {
  const AddOwnerPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Add Owner / Import'));
}
