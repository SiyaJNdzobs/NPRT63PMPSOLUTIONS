/// Driver — fare entry and departure confirmation.
/// After loading passengers, driver enters fare and taps Depart.
/// Writes a Trip record and removes the taxi from the queue.
/// TODO: implement form wired to FirestoreService.recordTrip.
import 'package:flutter/material.dart';
class FareDeparturePage extends StatelessWidget {
  const FareDeparturePage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Fare Entry & Depart'));
}
