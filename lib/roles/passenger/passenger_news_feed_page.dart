/// Passenger — news feed: active alerts from all ranks (or filtered by rank).
/// TODO: stream from FirestoreService.streamAlerts.
import 'package:flutter/material.dart';
class PassengerNewsFeedPage extends StatelessWidget {
  const PassengerNewsFeedPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Passenger News Feed'));
}
