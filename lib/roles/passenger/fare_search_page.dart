/// Passenger — fare and route search.
/// TODO: query Firestore ranks/taxis, display results.
import 'package:flutter/material.dart';
class FareSearchPage extends StatelessWidget {
  const FareSearchPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TODO: Fare / Route Search'));
}
